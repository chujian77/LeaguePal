name: Build and Release

on:
  push:
    tags:
      - 'v*' # 只有推送 v 开头的 tag 时触发
      
  workflow_dispatch: # 🌟 新增这一行！允许你在 GitHub 网页上手动点击按钮打包
jobs:
  release:
    runs-on: windows-latest # 仅使用 Windows 虚拟机

    permissions:
      contents: write

    steps:
      - name: Check out Git repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18 # 建议修改为你本地实际使用的 Node 版本 (如 18 或 20)

      - name: Install Dependencies
        run: npm install

      - name: Publish Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npm run publish